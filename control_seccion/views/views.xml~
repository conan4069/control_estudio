<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="control_seccion.list">
      <field name="name">control_seccion list</field>
      <field name="model">control_seccion.control_seccion</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="control_seccion.action_window">
      <field name="name">control_seccion window</field>
      <field name="res_model">control_seccion.control_seccion</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="control_seccion.action_server">
      <field name="name">control_seccion server</field>
      <field name="model_id" ref="model_control_seccion_control_seccion"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="control_seccion" id="control_seccion.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="control_seccion.menu_1" parent="control_seccion.menu_root"/>
    <menuitem name="Menu 2" id="control_seccion.menu_2" parent="control_seccion.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="control_seccion.menu_1_list" parent="control_seccion.menu_1"
              action="control_seccion.action_window"/>
    <menuitem name="Server to list" id="control_seccion" parent="control_seccion.menu_2"
              action="control_seccion.action_server"/>
    -->
  </data>
</odoo>